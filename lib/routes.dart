/// Typed paths used when constructing GoRouter routes.
class RoutesPath {
  static const home = '/';
  static const order = '/order';
}

/// Shared route names for push/pop calls throughout the app.
class RouteNames {
  static const home = 'home';
  static const order = 'order';
}
